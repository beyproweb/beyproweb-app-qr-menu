import './styles.css';

document.querySelector('#app').innerHTML = `
  <main class="shell">
    <section class="card">
      <p class="eyebrow">Beypro QR Menu</p>
      <h1>Local Vite setup is now wired correctly.</h1>
      <p class="copy">
        This project now resolves its own dependencies instead of falling back to a parent
        installation under /Users/nurikord/node_modules.
      </p>
      <p class="copy">
        Replace this placeholder with the QR menu implementation when you are ready.
      </p>
    </section>
  </main>
`;