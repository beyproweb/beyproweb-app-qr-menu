# app-qr-menu

Mobile app workspace for Beypro QR Menu customer app integration.

This repository now includes a minimal local Vite setup so development commands run from this
folder instead of resolving tooling from parent directories.

## Run locally

1. `npm install`
2. `npm run dev`

Replace the placeholder files in `src/` with the actual QR menu implementation.
